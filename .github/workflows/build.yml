name: Build CAMI APKs
on: [push, workflow_dispatch]

env:
  ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true

jobs:
  build:
    name: Build ${{ matrix.role }} APK
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        role: [camera, viewer]
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: ☕ Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: 17

      - name: 📱 Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: 📦 Installer dépendances
        run: npm ci

      - name: 🏗️ Build Vite + Initialiser Capacitor — ${{ matrix.role }}
        run: |
          if [ "${{ matrix.role }}" = "camera" ]; then
            npm run build:camera
            cp capacitor.config.camera.json capacitor.config.json
          else
            npm run build:viewer
            cp capacitor.config.viewer.json capacitor.config.json
          fi
          
          # ✅ Initialisation complète de Capacitor
          npx cap add android
          npx cap sync android
          
          # ✅ Corriger les permissions du script gradle
          chmod +x android/gradlew

      - name: 📱 Compiler APK
        working-directory: android
        run: ./gradlew assembleDebug --no-daemon

      - name: 📤 Renommer l'APK
        run: mv android/app/build/outputs/apk/debug/app-debug.apk cami-${{ matrix.role }}-debug.apk

      - name: 💾 Uploader l'APK
        uses: actions/upload-artifact@v4
        with:
          name: cami-${{ matrix.role }}-apk
          path: cami-${{ matrix.role }}-debug.apk
          retention-days: 30
